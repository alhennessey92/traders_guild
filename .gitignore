# Xcode user-specific data
*.xcuserstate
**/xcuserdata/

# Build artifacts
DerivedData/
build/
.codex-derived-data/

# macOS noise
.DS_Store

# Secrets / env
.env
.env.*
!.env.example
*.bak
*.backup

# Credential stores (never commit!)
*.kdbx
*.kdb

# Local verification builds (macOS port work)
.build-verify/

# Xcode derived data for the macOS target (any agent, any path)
.build-macos/
.build-*/
